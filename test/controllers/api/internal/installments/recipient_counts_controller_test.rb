# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (11 FactoryBot refs).
# Original: spec/controllers/api/internal/installments/recipient_counts_controller_spec.rb (deleted in this commit; see git history).
class Api::Internal::Installments::RecipientCountsControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/api/internal/installments/recipient_counts_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/api/internal/installments/recipient_counts_controller_spec.rb (11 FactoryBot refs) — see comment above"
  end
end
