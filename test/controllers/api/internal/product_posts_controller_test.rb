# frozen_string_literal: true

require "test_helper"

class Api::Internal::ProductPostsControllerTest < ActionController::TestCase
  test "TODO" do
    skip "TODO migrate (depends on 'with user signed in as admin for seller' shared context + seller_post factory + PaginatedProductPostsPresenter shape; needs team_memberships + installment fixtures)"
  end
end
