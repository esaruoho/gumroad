# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (113 FactoryBot refs).
# Original: spec/controllers/api/mobile/purchases_controller_spec.rb (deleted in this commit; see git history).
class Api::Mobile::PurchasesControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/api/mobile/purchases_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/api/mobile/purchases_controller_spec.rb (113 FactoryBot refs) — see comment above"
  end
end
