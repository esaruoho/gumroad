# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (32 FactoryBot refs).
# Original: spec/controllers/api/mobile/url_redirects_controller_spec.rb (deleted in this commit; see git history).
class Api::Mobile::UrlRedirectsControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/api/mobile/url_redirects_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/api/mobile/url_redirects_controller_spec.rb (32 FactoryBot refs) — see comment above"
  end
end
