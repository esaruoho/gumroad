# frozen_string_literal: true

require "test_helper"

class Api::Mobile::SubscriptionsControllerTest < ActionController::TestCase
  test "TODO" do
    skip "TODO migrate (uses VCR + subscription/credit_card/installment factory web; needs subscription_product, credit_card, subscription, original purchase, installments fixtures)"
  end
end
