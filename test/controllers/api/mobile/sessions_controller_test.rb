# frozen_string_literal: true

require "test_helper"

class Api::Mobile::SessionsControllerTest < ActionController::TestCase
  test "TODO" do
    skip "TODO migrate (uses VCR + doorkeeper/access_token factory with mobile_api scope; needs oauth_application + Doorkeeper::AccessToken fixture)"
  end
end
