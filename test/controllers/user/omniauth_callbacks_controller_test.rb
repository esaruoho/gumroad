# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (31 FactoryBot refs).
# Original: spec/controllers/user/omniauth_callbacks_controller_spec.rb (deleted in this commit; see git history).
class User::OmniauthCallbacksControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/user/omniauth_callbacks_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/user/omniauth_callbacks_controller_spec.rb (31 FactoryBot refs) — see comment above"
  end
end
