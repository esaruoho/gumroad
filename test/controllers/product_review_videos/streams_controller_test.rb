# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (1 FactoryBot refs).
# Original: spec/controllers/product_review_videos/streams_controller_spec.rb (deleted in this commit; see git history).
class ProductReviewVideos::StreamsControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/product_review_videos/streams_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/product_review_videos/streams_controller_spec.rb (1 FactoryBot refs) — see comment above"
  end
end
