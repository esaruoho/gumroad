# frozen_string_literal: true

require "test_helper"

class LicensesControllerTest < ActionController::TestCase
  test "TODO" do
    skip "TODO migrate (depends on 'with user signed in as admin for seller' shared context + license factory + Audience::PurchasePolicy route-iteration shared example; needs license + linked purchase fixtures)"
  end
end
