# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (5 FactoryBot refs).
# Original: spec/controllers/bundles/share_controller_spec.rb (deleted in this commit; see git history).
class Bundles::ShareControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/bundles/share_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/bundles/share_controller_spec.rb (5 FactoryBot refs) — see comment above"
  end
end
