# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (1 FactoryBot refs).
# Original: spec/controllers/concerns/pundit_authorization_spec.rb (deleted in this commit; see git history).
class PunditAuthorizationConcernTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/concerns/pundit_authorization_spec.rb" do
    skip "TODO: migrate spec/controllers/concerns/pundit_authorization_spec.rb (1 FactoryBot refs) — see comment above"
  end
end
