# frozen_string_literal: true

require "test_helper"

class Admin::Users::StatsControllerTest < ActionController::TestCase
  test "TODO" do
    skip "TODO migrate (stats endpoint requires stubbing User instance methods + PurchaseSearchService; no clean Minitest path without prepend or any_instance)"
  end
end
