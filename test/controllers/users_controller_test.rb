# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (45 FactoryBot refs).
# Original: spec/controllers/users_controller_spec.rb (deleted in this commit; see git history).
class UsersControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/users_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/users_controller_spec.rb (45 FactoryBot refs) — see comment above"
  end
end
