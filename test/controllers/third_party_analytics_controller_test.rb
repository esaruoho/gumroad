# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (12 FactoryBot refs).
# Original: spec/controllers/third_party_analytics_controller_spec.rb (deleted in this commit; see git history).
class ThirdPartyAnalyticsControllerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/controllers/third_party_analytics_controller_spec.rb" do
    skip "TODO: migrate spec/controllers/third_party_analytics_controller_spec.rb (12 FactoryBot refs) — see comment above"
  end
end
