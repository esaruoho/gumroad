# frozen_string_literal: true

require "test_helper"

class ConfirmedFollowerEventTest < ActiveSupport::TestCase
  test "todo" do
    skip "TODO: migrate (reason: requires real Elasticsearch for EsClient.index/get round-trip; fake_es stub returns noop, so doc retrieval cannot be asserted)"
  end
end
