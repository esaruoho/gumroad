# frozen_string_literal: true

require "test_helper"

class ThumbnailTest < ActiveSupport::TestCase
  test "todo" do
    skip "TODO: migrate (reason: ActiveStorage S3 attachments via fixture_file_upload + blob.analyze; pitfall: Makara::Errors::BlacklistedWhileInTransaction in CI)"
  end
end
