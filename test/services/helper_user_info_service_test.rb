# frozen_string_literal: true

require "test_helper"

class HelperUserInfoServiceTest < ActiveSupport::TestCase
  test "TODO: migrate spec/services/helper_user_info_service_spec.rb (22 FB refs)" do
    skip "Awaiting fixtures migration: uses index_model_records(Purchase) (Elasticsearch reindex) for value-calc branch + recent-purchase + suspended/affiliate states. ES-backed; defer per skill guidance."
  end
end
