# frozen_string_literal: true

require "test_helper"

class GdprDataErasureServiceTest < ActiveSupport::TestCase
  test "skip" do
    skip "TODO migrate spec/services/gdpr_data_erasure_service_spec.rb (10 FB refs but cross-tables: carts/credit_cards/devices/products/comments + allow on instance)"
  end
end
