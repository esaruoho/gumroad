# frozen_string_literal: true

require "test_helper"

class BestOfferCodeServiceTest < ActiveSupport::TestCase
  # Sharpened skip-stub.
  # Original: spec/services/best_offer_code_service_spec.rb
  # Blocker: 60 FactoryBot refs; bundle/universal offer code resolution across product+variant+recurrence+currency; service walks live AR scopes.
  test "TODO: migrate spec/services/best_offer_code_service_spec.rb" do
    skip "Fixture-hostile — see top-of-file blocker note"
  end
end
