# frozen_string_literal: true

require "test_helper"

class PriceCheckerServiceTest < ActiveSupport::TestCase
  test "TODO: migrate spec/services/price_checker_service_spec.rb (17 FB refs, Taxonomy seed + Elasticsearch index_model_records)" do
    skip "Skip-batch: Taxonomy.find_or_create_by + Elasticsearch search + index_model_records"
  end
end
