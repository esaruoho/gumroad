# frozen_string_literal: true

require "test_helper"

class PaypalBalanceCheckServiceTest < ActiveSupport::TestCase
  test "TODO: migrate spec/services/paypal_balance_check_service_spec.rb (needs Payment + Balance fixtures with user/date/amount_cents)" do
    skip "Awaiting fixtures migration: requires Payment + Balance fixture family (none currently in test/fixtures/) and PaypalPayoutProcessor stubbing for current_paypal_balance_cents/topup_amount_in_transit"
  end
end
