# frozen_string_literal: true

require "test_helper"

class Admin::RelatedUsersServiceTest < ActiveSupport::TestCase
  test "skip" do
    skip "TODO migrate spec/services/admin/related_users_service_spec.rb (35 FB refs + many users with credit_card fingerprints + updated_at controls)"
  end
end
