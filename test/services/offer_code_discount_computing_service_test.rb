# frozen_string_literal: true

require "test_helper"

class OfferCodeDiscountComputingServiceTest < ActiveSupport::TestCase
  # Sharpened skip-stub.
  # Original: spec/services/offer_code_discount_computing_service_spec.rb
  # Blocker: 36 FactoryBot refs; relies on product/variant pricing + universal/multi-product offer codes + currency + recurrence interactions through the live discount computation pipeline.
  test "TODO: migrate spec/services/offer_code_discount_computing_service_spec.rb" do
    skip "Fixture-hostile — see top-of-file blocker note"
  end
end
