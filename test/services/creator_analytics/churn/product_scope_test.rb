# frozen_string_literal: true

require "test_helper"

class CreatorAnalytics::Churn::ProductScopeTest < ActiveSupport::TestCase
  test "skip" do
    skip "TODO migrate spec/services/creator_analytics/churn/product_scope_spec.rb (14 FB refs + memoize-receive expectations + products_for_creator_analytics + membership/subscription product factories)"
  end
end
