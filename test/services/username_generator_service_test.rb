# frozen_string_literal: true

require "test_helper"

class UsernameGeneratorServiceTest < ActiveSupport::TestCase
  test "TODO: migrate spec/services/username_generator_service_spec.rb (VCR cassettes for OpenAI completions + extensive partial-double stubbing)" do
    skip "Awaiting fixtures migration: depends on VCR which isn't wired into Minitest harness yet"
  end
end
