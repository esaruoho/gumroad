# frozen_string_literal: true

require "test_helper"

class BundleSearchProductsServiceTest < ActiveSupport::TestCase
  # Sharpened skip-stub.
  # Original: spec/services/bundle_search_products_service_spec.rb
  # Blocker: 17 FactoryBot refs + Elasticsearch search via Link::Searchable index against recommendable trait users. ES indexing helper unavailable in Minitest lane.
  test "TODO: migrate spec/services/bundle_search_products_service_spec.rb" do
    skip "Fixture-hostile — see top-of-file blocker note"
  end
end
