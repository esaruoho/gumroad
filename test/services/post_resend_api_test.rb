# frozen_string_literal: true

require "test_helper"

class PostResendApiTest < ActiveSupport::TestCase
  test "TODO: migrate spec/services/post_resend_api_spec.rb (25 FB refs, Premailer asset lookup + seller_profile + product_file + url_redirect)" do
    skip "Skip-batch: Premailer asset pipeline (pitfall #13), seller_profile customization, deep mail rendering"
  end
end
