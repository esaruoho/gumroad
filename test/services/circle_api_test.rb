# frozen_string_literal: true

require "test_helper"

class CircleApiTest < ActiveSupport::TestCase
  test "TODO" do
    skip "migrate from spec/services/circle_api_spec.rb " \
         "(VCR-driven integration spec: replays recorded HTTP cassettes against real Circle " \
         "communities/space_groups/members endpoints; needs cassette replay strategy or WebMock " \
         "stubs translation before Minitest port)"
  end
end
