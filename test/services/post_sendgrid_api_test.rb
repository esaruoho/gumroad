# frozen_string_literal: true

require "test_helper"

class PostSendgridApiTest < ActiveSupport::TestCase
  # Sharpened skip-stub.
  # Original: spec/services/post_sendgrid_api_spec.rb
  # Blocker: 25 FactoryBot refs; full email rendering with seller design, CTA, attachments via SendGrid; same as post_resend_api but targets SendGrid client API.
  test "TODO: migrate spec/services/post_sendgrid_api_spec.rb" do
    skip "Fixture-hostile — see top-of-file blocker note"
  end
end
