# frozen_string_literal: true

require "test_helper"

class NotionApiTest < ActiveSupport::TestCase
  test "TODO: migrate spec/services/notion_api_spec.rb (VCR cassette + GlobalConfig stubs)" do
    skip "Awaiting fixtures migration: depends on VCR which isn't wired into Minitest harness yet"
  end
end
