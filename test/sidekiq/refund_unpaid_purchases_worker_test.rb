# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (4 FactoryBot refs).
# Original: spec/sidekiq/refund_unpaid_purchases_worker_spec.rb (deleted in this commit; see git history).
class RefundUnpaidPurchasesWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/refund_unpaid_purchases_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/refund_unpaid_purchases_worker_spec.rb (4 FactoryBot refs) — see comment above"
  end
end
