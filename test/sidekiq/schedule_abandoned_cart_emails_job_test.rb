# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (48 FactoryBot refs).
# Original: spec/sidekiq/schedule_abandoned_cart_emails_job_spec.rb (deleted in this commit; see git history).
class ScheduleAbandonedCartEmailsJobTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/schedule_abandoned_cart_emails_job_spec.rb" do
    skip "TODO: migrate spec/sidekiq/schedule_abandoned_cart_emails_job_spec.rb (48 FactoryBot refs) — see comment above"
  end
end
