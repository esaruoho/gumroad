# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (15 FactoryBot refs).
# Original: spec/sidekiq/remove_deleted_files_from_s3_job_spec.rb (deleted in this commit; see git history).
class RemoveDeletedFilesFromS3JobTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/remove_deleted_files_from_s3_job_spec.rb" do
    skip "TODO: migrate spec/sidekiq/remove_deleted_files_from_s3_job_spec.rb (15 FactoryBot refs) — see comment above"
  end
end
