# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (78 FactoryBot refs).
# Original: spec/sidekiq/stripe_create_merchant_accounts_worker_spec.rb (deleted in this commit; see git history).
class StripeCreateMerchantAccountsWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/stripe_create_merchant_accounts_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/stripe_create_merchant_accounts_worker_spec.rb (78 FactoryBot refs) — see comment above"
  end
end
