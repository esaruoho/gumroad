# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (5 FactoryBot refs).
# Original: spec/sidekiq/send_payment_reminder_worker_spec.rb (deleted in this commit; see git history).
class SendPaymentReminderWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/send_payment_reminder_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/send_payment_reminder_worker_spec.rb (5 FactoryBot refs) — see comment above"
  end
end
