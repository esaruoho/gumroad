# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (56 FactoryBot refs).
# Original: spec/sidekiq/recurring_charge_worker_spec.rb (deleted in this commit; see git history).
class RecurringChargeWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/recurring_charge_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/recurring_charge_worker_spec.rb (56 FactoryBot refs) — see comment above"
  end
end
