# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (12 FactoryBot refs).
# Original: spec/sidekiq/unsubscribe_and_fail_worker_spec.rb (deleted in this commit; see git history).
class UnsubscribeAndFailWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/unsubscribe_and_fail_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/unsubscribe_and_fail_worker_spec.rb (12 FactoryBot refs) — see comment above"
  end
end
