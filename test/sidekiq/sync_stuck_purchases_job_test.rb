# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (21 FactoryBot refs).
# Original: spec/sidekiq/sync_stuck_purchases_job_spec.rb (deleted in this commit; see git history).
class SyncStuckPurchasesJobTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/sync_stuck_purchases_job_spec.rb" do
    skip "TODO: migrate spec/sidekiq/sync_stuck_purchases_job_spec.rb (21 FactoryBot refs) — see comment above"
  end
end
