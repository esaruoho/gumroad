# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (46 FactoryBot refs).
# Original: spec/sidekiq/sync_stuck_payouts_job_spec.rb (deleted in this commit; see git history).
class SyncStuckPayoutsJobTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/sync_stuck_payouts_job_spec.rb" do
    skip "TODO: migrate spec/sidekiq/sync_stuck_payouts_job_spec.rb (46 FactoryBot refs) — see comment above"
  end
end
