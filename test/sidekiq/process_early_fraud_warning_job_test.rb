# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (8 FactoryBot refs).
# Original: spec/sidekiq/process_early_fraud_warning_job_spec.rb (deleted in this commit; see git history).
class ProcessEarlyFraudWarningJobTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/process_early_fraud_warning_job_spec.rb" do
    skip "TODO: migrate spec/sidekiq/process_early_fraud_warning_job_spec.rb (8 FactoryBot refs) — see comment above"
  end
end
