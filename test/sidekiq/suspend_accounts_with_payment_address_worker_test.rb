# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (20 FactoryBot refs).
# Original: spec/sidekiq/suspend_accounts_with_payment_address_worker_spec.rb (deleted in this commit; see git history).
class SuspendAccountsWithPaymentAddressWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/suspend_accounts_with_payment_address_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/suspend_accounts_with_payment_address_worker_spec.rb (20 FactoryBot refs) — see comment above"
  end
end
