# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (38 FactoryBot refs).
# Original: spec/sidekiq/utm_link_sale_attribution_job_spec.rb (deleted in this commit; see git history).
class UtmLinkSaleAttributionJobTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/utm_link_sale_attribution_job_spec.rb" do
    skip "TODO: migrate spec/sidekiq/utm_link_sale_attribution_job_spec.rb (38 FactoryBot refs) — see comment above"
  end
end
