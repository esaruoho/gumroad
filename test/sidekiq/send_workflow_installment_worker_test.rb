# frozen_string_literal: true

require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration
# because of factory/Stripe/HTTP/ES dependencies (23 FactoryBot refs).
# Original: spec/sidekiq/send_workflow_installment_worker_spec.rb (deleted in this commit; see git history).
class SendWorkflowInstallmentWorkerTest < ActiveSupport::TestCase
  test "TODO: migrate spec/sidekiq/send_workflow_installment_worker_spec.rb" do
    skip "TODO: migrate spec/sidekiq/send_workflow_installment_worker_spec.rb (23 FactoryBot refs) — see comment above"
  end
end
