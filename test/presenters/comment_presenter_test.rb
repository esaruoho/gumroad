require "test_helper"

# TODO: Migrate from RSpec. This spec was skip-batched during the bulk
# fixtures-only migration. Reason: Uses :with_avatar trait (ActiveStorage attach) — out of scope for fixtures-only migration.
#
# Original spec: spec/presenters/comment_presenter_spec.rb (deleted in this commit; see git history). FB ref count: 11.
class CommentPresenterTest < ActiveSupport::TestCase
  test "TODO: migrate from RSpec — fixture-hostile, requires manual rewrite" do
    skip "TODO: migrate spec/presenters/comment_presenter_spec.rb (11 FactoryBot refs)"
  end
end
