require "test_helper"

# TODO: Migrate from RSpec. This spec was skip-batched during the bulk
# fixtures-only migration because it has 50 FactoryBot/create references
# (and/or fixture-hostile dependencies) — too coupled to convert mechanically.
# Reason: Pre-authorized skip-batch (>40 FactoryBot refs) plus ActiveStorage attach.
#
# Original spec: spec/presenters/customer_presenter_spec.rb (deleted in this commit; see git history)
class CustomerPresenterTest < ActiveSupport::TestCase
  test "TODO: migrate from RSpec — fixture-hostile, requires manual rewrite" do
    skip "TODO: migrate spec/presenters/customer_presenter_spec.rb (50 FactoryBot refs)"
  end
end
