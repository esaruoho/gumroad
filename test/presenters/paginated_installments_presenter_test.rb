require "test_helper"

class PaginatedInstallmentsPresenterTest < ActiveSupport::TestCase
  test "TODO migrate" do
    skip "TODO: migrate (depends on InstallmentPresenter which is itself skip-batched; needs installment_rules.yml + InstallmentSearchService stubbing)"
  end
end
