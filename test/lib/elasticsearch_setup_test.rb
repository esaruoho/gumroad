require "test_helper"

# TODO: Migrate from RSpec. Skip-batched during the bulk fixtures-only migration:
# ElasticsearchSetup is a spec/support helper class, not in lib/. Out of scope
# for the spec/→test/ conversion until the support class is ported.
#
# Original spec: spec/lib/elasticsearch_setup_spec.rb (deleted in this commit; see git history)
class ElasticsearchSetupTest < ActiveSupport::TestCase
  test "TODO: migrate from RSpec — spec/support class, out of scope" do
    skip "TODO: migrate spec/lib/elasticsearch_setup_spec.rb — ElasticsearchSetup is a spec/support helper, not a lib/ class"
  end
end
